import * as React from "react"
import styled from "styled-components"
// import { GatsbyImage } from "gatsby-plugin-image"

export default function CardPromote() {
  return (
    <CardPromoteStyles>
    </CardPromoteStyles>
  )
}

const CardPromoteStyles = styled.div`
  @media (min-width: 640px) {
  }
`
