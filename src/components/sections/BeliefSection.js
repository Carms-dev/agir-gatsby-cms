import * as React from "react"
import styled from "styled-components"

export default function BeliefSection({ belief }) {
  return (
    <BeliefSectionStyles>
    {/* TODO */}
    </BeliefSectionStyles>
  )
}

const BeliefSectionStyles = styled.div`

  @media (min-width: 640px) {
  }
`
